`erl_env`
=========

This CMake project provides `erl_env` which is a collection of environment abstractions.
This package is an updated version of another [repo](https://github.com/ExistentialRobotics/erl_env).
We should merge the old one into this one in the future.
